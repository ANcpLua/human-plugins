# cc8

Creates one tmux session with eight Claude/Codex workspaces, either as separate
windows or a tiled 4×2 view.

```bash
cc8
cc8 grid
cc8 status
cc8 kill
```

Requires Bash and tmux. `CC8_SESSION` changes the tmux session name.
